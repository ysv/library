<p id="notice"><%= notice %></p>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th>Pages</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.date %></td>
        <td><%= book.pages %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>

<p id="notice"><%= notice %></p>

<h1>Authors</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Born</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.name %></td>
        <td><%= author.born %></td>
        <td><%= link_to 'Show', author %></td>
        <td><%= link_to 'Edit', edit_author_path(author) %></td>
        <td><%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path %>

<p id="notice"><%= notice %></p>

<h1>Catalogs</h1>

<table>
  <thead>
    <tr>
      <th>Folder</th>
      <th>Type</th>
      <th>Name</th>
      <th>Characteristic</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catalogs.each do |catalog| %>
      <tr>
        <td><%= catalog.folder %></td>
        <td><%= catalog.type %></td>
        <td><%= catalog.name %></td>
        <td><%= catalog.characteristic %></td>
        <td><%= link_to 'Show', catalog %></td>
        <td><%= link_to 'Edit', edit_catalog_path(catalog) %></td>
        <td><%= link_to 'Destroy', catalog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Catalog', new_catalog_path %>

<p id="notice"><%= notice %></p>

<h1>Shels</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Row</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @shels.each do |shel| %>
      <tr>
        <td><%= shel.name %></td>
        <td><%= shel.row %></td>
        <td><%= link_to 'Show', shel %></td>
        <td><%= link_to 'Edit', edit_shel_path(shel) %></td>
        <td><%= link_to 'Destroy', shel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Shel', new_shel_path %>


<p id="notice"><%= notice %></p>

<h1>Library Rows</h1>

<table>
  <thead>
    <tr>
      <th>Book</th>
      <th>Catalog</th>
      <th>Shel</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @library_rows.each do |library_row| %>
      <tr>
        <td><%= library_row.book_id %></td>
        <td><%= library_row.catalog_id %></td>
        <td><%= library_row.shel_id %></td>
        <td><%= link_to 'Show', library_row %></td>
        <td><%= link_to 'Edit', edit_library_row_path(library_row) %></td>
        <td><%= link_to 'Destroy', library_row, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Library Row', new_library_row_path %>

<p id="notice"><%= notice %></p>

<h1>Books Authors</h1>

<table>
  <thead>
    <tr>
      <th>Book</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books_authors.each do |books_author| %>
      <tr>
        <td><%= books_author.book_id %></td>
        <td><%= books_author.author_id %></td>
        <td><%= link_to 'Show', books_author %></td>
        <td><%= link_to 'Edit', edit_books_author_path(books_author) %></td>
        <td><%= link_to 'Destroy', books_author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Books Author', new_books_author_path %>

