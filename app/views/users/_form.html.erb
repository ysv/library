<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :id %>
    <%= form.number_field :id, id: :id %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.text_field :day, id: :user_day %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :user_name %>
  </div>

  <div class="field">
    <%= form.label :favourite %>
    <%= form.text_field :favourite, id: :user_favourite %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
